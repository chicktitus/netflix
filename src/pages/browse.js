import React from 'react';
import {useContent} from '../hooks/use-content';

export default function Browse(){
  // we need the series and films
  // we need slides
  //pass it to the browse contaainer
  return <p>hello this is Browse page</p>
};
